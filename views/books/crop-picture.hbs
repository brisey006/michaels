                        <div class="row justify-content-center">
                            <div class="col-12 col-lg-8 col-xl-6">
                                <div class="card">
                                    <div class="card-body">
                                        <h3>Crop book cover</h3>
                                        <img class="card-img-top" style="height: 300px;" src="{{book.tmpCoverUrl}}" id="croppr" alt="{{book.fullName}}">
                                        <img class="card-img-top" style="display: none; height: 300px;" src="{{book.tmpCoverUrl}}" id="imgr" alt="{{book.fullName}}">
                                        <canvas id="myCanvas" style="display: none"></canvas>
                                        <div class="clearfix text-right mt-3">
                                            <button id="save" class="btn btn-danger"> <i class="mdi mdi-content-save mr-1"></i> Save</button>
                                        </div>
                                    </div> <!-- end card-body-->
                                </div> <!-- end card-->
                            </div><!-- end col -->
                        </div>
                        <input type="hidden" id="profileUrl" value="{{parseUrl 'book' urls id=book._id}}">
                        <!-- end row -->  